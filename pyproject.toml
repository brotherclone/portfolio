[project]
name = "portfolio"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["backend/entities.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
